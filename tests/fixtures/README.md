# Test fixtures

Each subdirectory here is a fake marketplace state used by `tests/test_validate_marketplace.py`. Fixtures intentionally include broken cases (duplicate names, missing folders, etc.) so the validator can be tested against them.

Do not add real plugins here.
